Always generate shell commands for Windows PowerShell.
Use `Get-Content` instead of `cat`, `dir` instead of `ls`.
&& is not allowed in PowerShell
Run tests with `cargo nextest run --status-level=fail`. Do not use `| cat` to run the tests.